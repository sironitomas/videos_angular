import { AppMocksResourceHandler } from './app-mocks-resource-handler';

describe('AppMocksResourceHandler', () => {
  it('should create an instance', () => {
    expect(new AppMocksResourceHandler(null)).toBeTruthy();
  });
});
