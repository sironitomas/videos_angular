import { MessagesErrorHandler } from './messages-error-handler';

describe('MessagesErrorHandler', () => {
  it('should create an instance', () => {
    expect(new MessagesErrorHandler()).toBeTruthy();
  });
});
